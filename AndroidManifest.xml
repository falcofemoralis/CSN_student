<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ksm_2_course">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OBG"></activity>
        <activity android:name=".S_metod_prog" />
        <activity android:name=".DataBase" />
        <activity android:name=".Arch_Comp" />
        <activity android:name=".CompScheme" />
        <activity android:name=".Alg_and_metod" />
        <activity android:name=".Lessons_schedule" />
        <activity android:name=".Lessons_schedule_2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>